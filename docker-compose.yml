services:
  genetic_flask:
    container_name: genetic
    build:
      context: ./app
      target: prod
    environment:
      FLASK_DEBUG: 1
    volumes:
    - ./app/:/work
    ports:
      - 5000:5000
      - 5678:5678